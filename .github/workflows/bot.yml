name: Crypto Signal Bot

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

concurrency:
  group: crypto-bot
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run bot
        env:
          LBANK_API_KEY: ${{ secrets.LBANK_API_KEY }}
          LBANK_SECRET_KEY: ${{ secrets.LBANK_SECRET_KEY }}
          LBANK_PASSPHRASE: ${{ secrets.LBANK_PASSPHRASE }}
        run: python main.py

      - name: Save memory file
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add last_alerts.json trading_state.json || echo "No files"
          git commit -m "Update state files" || echo "No changes"
          git push || echo "No push needed"
